{
  "name": "gateway",
  "version": "1.0.0",
  "description": "gateway",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js",
    "dev": "nodemon server.js",
    "deploy": "heroku container:push web -a notes-gateway && heroku container:release web -a notes-gateway"
  },
  "keywords": [
    "gateway"
  ],
  "author": "Fouad Bakkour",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.1",
    "express-rate-limit": "^6.4.0",
    "http-proxy-middleware": "^2.0.6",
    "morgan": "^1.10.0"
  },
  "devDependencies": {
    "nodemon": "^2.0.16"
  }
}
